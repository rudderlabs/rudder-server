<!-- regulation-worker needs config-backend-url & config-backend-token as env.
So, if running it locally, store `CONFIG_BACKEND_URL` & `CONFIG_BACKEND_TOKEN`
variables with url & token in ./cmd/.env file-->
